<h1 align="center">WASAText</h1>
<p align="center">Repository for the WASAText project, for the WASA course @ Sapienza (A.Y. 2024/25)</p>

---

# TODO List

- [ ] Build the API, which should comprehend the following endpoints:

|API Endpoint|OpenAPI|Go (Backend)|Vue (Frontend)|
|---|---|---|---|
|`doLogin` (see simplified login)| × | × | × |
|`setMyUserName`| × | × | × |
|`getMyConversations`| × | × | × |
|`getConversation`| × | × | × |
|`sendMessage`| × | × | × |
|`forwardMessage`| × | × | × |
|`commentMessage`| × | × | × |
|`uncommentMessage`| × | × | × |
|`deleteMessage`| × | × | × |
|`addToGroup`| × | × | × |
|`leaveGroup`| × | × | × |
|`setGroupName`| × | × | × |
|`setMyPhoto`| × | × | × |
|`setGroupPhoto`| × | × | × |

- [ ] Build the backend;
- [ ] Build the frontend;
- [ ] Dockerize the application.