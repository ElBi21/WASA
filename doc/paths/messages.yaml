parameters:
  - name: id
    description: |
      ID of the user
    in: path
    required: true
    schema:
      type: integer
      minimum: 1
      maximum: 2048
  - name: chatID
    description: |
      ID of the chat
    in: path
    required: true
    schema:
      type: integer
      minimum: 1
      maximum: 65536
  - name: messageID
    description: |
      ID of the message
    in: path
    required: true
    schema:
      type: integer
      minimum: 1
      maximum: 1048576

post:
  tags: ["Chats"]
  summary: Forward the message
  operationId: forwardMessage