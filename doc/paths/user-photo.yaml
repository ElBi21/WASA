parameters:
  - name: id
    description: |
      ID of the user
    in: path
    required: true
    schema:
      type: integer
      minimum: 1
      maximum: 2048

post:
  tags: ["User operations"]
  summary: Changes the user's photo
  operationId: setMyPhoto